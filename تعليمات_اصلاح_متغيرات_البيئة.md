# تعليمات إصلاح متغيرات البيئة للمشروع

## 📋 ملخص التحديثات

تم إصلاح المشكلة الأساسية المتعلقة باستخدام مسارات API ثابتة في الكود. الآن جميع مسارات API يتم قراءتها من متغيرات البيئة في ملف `.env`.

## ✅ التغييرات المنفذة

### 1. الملفات المحدثة

| الملف | التغيير | الحالة |
|-------|---------|--------|
| `src/config/security.ts` | تحديث دالة `getApiUrl()` لقراءة `VITE_API_URL` مباشرة | ✅ |
| `src/services/api.js` | استخدام `SECURE_API_CONFIG` بدلاً من المسار الثابت | ✅ |
| `src/lib/api.ts` | تبسيط دالة `getApiBase()` لاستخدام `SECURE_API_CONFIG` | ✅ |
| `src/config/environment.ts` | تحويل `API_URL` إلى getter ديناميكي | ✅ |
| `src/pages/VendorSignup.tsx` | استخدام متغيرات البيئة في fetch | ✅ |
| `.env` | إنشاء ملف البيئة من `env.local` | ✅ |

### 2. الملفات الجديدة

- ✅ `ENV_VARIABLES_GUIDE.md` - دليل شامل باللغتين
- ✅ `QUICK_FIX_API_ENV.md` - ملخص سريع للإصلاح
- ✅ `test-env-config.bat` - سكريبت اختبار التكوين

## 🚀 كيفية الاستخدام

### الخطوة 1: تحديث ملف `.env`

افتح ملف `.env` في جذر المشروع وتأكد من وجود:

```env
# للتطوير المحلي
VITE_API_URL=http://localhost:8000/api/v1
VITE_API_MODE=direct

# أو للإنتاج
VITE_API_URL=https://adminxd.eliteonegrocery.com/api/v1
VITE_API_MODE=direct
```

### الخطوة 2: تشغيل سكريبت الاختبار (اختياري)

```bash
test-env-config.bat
```

سيقوم هذا السكريبت بـ:
- التحقق من وجود ملف `.env`
- عرض قيم متغيرات API
- التأكد من صحة التكوين

### الخطوة 3: إعادة تشغيل خادم التطوير

```bash
# أوقف الخادم الحالي (Ctrl+C)

# أعد تشغيله
npm run dev
```

⚠️ **مهم:** يجب إعادة تشغيل الخادم بعد أي تعديل في ملف `.env`

### الخطوة 4: التحقق من التكوين

1. افتح التطبيق في المتصفح
2. اضغط F12 لفتح Developer Tools
3. انتقل إلى تبويب Console
4. ابحث عن الرسائل التالية:

```
🔍 Security Config - VITE_API_URL: http://localhost:8000/api/v1
✅ Security Config - Using VITE_API_URL from environment: http://localhost:8000/api/v1
🌐 API URL initialized from SECURE_API_CONFIG: http://localhost:8000/api/v1
```

## 📊 مخطط تدفق البيانات

```
┌─────────────────┐
│   ملف .env      │
│                 │
│ VITE_API_URL=   │
│ http://...      │
└────────┬────────┘
         │
         ↓
┌─────────────────────────────┐
│ src/config/security.ts      │
│                             │
│ getApiUrl()                 │
│ ├─ يقرأ VITE_API_URL        │
│ └─ يخزن في cachedApiUrl     │
└────────┬────────────────────┘
         │
         ↓
┌─────────────────────────────┐
│ SECURE_API_CONFIG.API_URL   │
└────────┬────────────────────┘
         │
         ├──────────────┬──────────────┬─────────────┐
         ↓              ↓              ↓             ↓
   ┌─────────┐   ┌──────────┐   ┌─────────┐   ┌──────────┐
   │ api.js  │   │ api.ts   │   │ lib/api │   │ ملفات    │
   │         │   │ (config) │   │         │   │ أخرى     │
   └─────────┘   └──────────┘   └─────────┘   └──────────┘
```

## 🔍 التحقق من حل المشكلة

### المشكلة الأصلية:
```
❌ GET http://localhost:8000/api/v1/... net::ERR_CONNECTION_REFUSED
```

### الحل:

1. **إذا كان الخادم الخلفي على منفذ مختلف:**
   ```env
   VITE_API_URL=http://localhost:8080/api/v1
   ```

2. **إذا كان الخادم الخلفي على نطاق مختلف:**
   ```env
   VITE_API_URL=https://api.example.com/api/v1
   ```

3. **للتطوير مع proxy:**
   ```env
   VITE_API_MODE=proxy
   VITE_API_TARGET=http://localhost:8000
   ```

## 🛠️ استكشاف الأخطاء وإصلاحها

### مشكلة 1: لا تزال تظهر المسارات القديمة

**الحل:**
```bash
# امسح cache
npm run clean

# احذف مجلد node_modules/.vite
rmdir /s /q node_modules\.vite

# أعد التشغيل
npm run dev
```

### مشكلة 2: `undefined` في Console

**السبب:** ملف `.env` غير موجود أو فارغ

**الحل:**
```bash
# انسخ من env.local
copy env.local .env

# تحقق من المحتوى
type .env
```

### مشكلة 3: التغييرات لا تظهر

**السبب:** لم يتم إعادة تشغيل الخادم

**الحل:**
```bash
# أوقف الخادم (Ctrl+C)
# أعد التشغيل
npm run dev
```

### مشكلة 4: أخطاء CORS

**الحل 1 - استخدم proxy:**
```env
VITE_API_MODE=proxy
```

**الحل 2 - تحديث إعدادات CORS في Laravel:**
```php
// config/cors.php
'allowed_origins' => ['http://localhost:5174'],
```

## 📝 أمثلة لسيناريوهات مختلفة

### سيناريو 1: خادم محلي على منفذ 8000
```env
VITE_API_URL=http://localhost:8000/api/v1
VITE_API_MODE=direct
```

### سيناريو 2: خادم محلي على منفذ 8080
```env
VITE_API_URL=http://localhost:8080/api/v1
VITE_API_MODE=direct
```

### سيناريو 3: خادم إنتاج
```env
VITE_API_URL=https://api.example.com/api/v1
VITE_API_MODE=direct
```

### سيناريو 4: استخدام proxy للتطوير
```env
VITE_API_MODE=proxy
VITE_API_TARGET=http://localhost:8000
VITE_API_URL=http://localhost:5174/api/v1
```

## 🎯 فوائد التحديث

1. ✅ **مرونة كاملة:** غيّر المسار من مكان واحد فقط
2. ✅ **سهولة التطوير:** لا حاجة لتعديل الكود لكل بيئة
3. ✅ **أمان أفضل:** لا مسارات حساسة في الكود
4. ✅ **نشر أسهل:** إعدادات مختلفة لكل بيئة
5. ✅ **صيانة أفضل:** كود أنظف وأسهل في القراءة

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. راجع ملف `ENV_VARIABLES_GUIDE.md` للدليل الشامل
2. راجع ملف `QUICK_FIX_API_ENV.md` للملخص السريع
3. شغل `test-env-config.bat` للتحقق من التكوين
4. تحقق من رسائل Console في المتصفح

## ✨ الخلاصة

الآن التطبيق:
- ✅ يستخدم متغيرات البيئة فقط (لا مسارات ثابتة)
- ✅ يدعم بيئات متعددة (تطوير/إنتاج)
- ✅ يوفر رسائل تصحيح واضحة
- ✅ سهل التكوين والصيانة

**ملاحظة نهائية:** تذكر دائماً إعادة تشغيل خادم التطوير بعد تعديل ملف `.env`!






