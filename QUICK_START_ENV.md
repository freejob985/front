# تعليمات سريعة - إصلاح مشكلة متغيرات البيئة

## ✅ تم الإصلاح!

### ما تم عمله:
1. **إنشاء ملف `.env`** في مجلد `Front` مع رابط `engeb.com`
2. **تحديث الكود** ليستخدم متغيرات البيئة بدلاً من الرابط المباشر
3. **إضافة تعليقات توضيحية** في الكود

### الملفات المحدثة:
- ✅ `Front/.env` - ملف متغيرات البيئة
- ✅ `Front/src/lib/api.ts` - يستخدم `import.meta.env.VITE_API_URL`
- ✅ `Front/src/services/api.js` - يستخدم `import.meta.env.VITE_API_URL`

## 🚀 كيفية التشغيل:

### 1. تأكد من وجود ملف `.env`:
```bash
cd Front
ls .env  # يجب أن ترى الملف
```

### 2. أعد تشغيل خادم التطوير:
```bash
npm run dev
```

### 3. اختبر تسجيل الدخول:
- اذهب إلى `http://localhost:5174/login`
- جرب تسجيل الدخول
- يجب أن يعمل الآن مع رابط `engeb.com`

## 🔍 للتحقق من الإعدادات:

افتح `Front/test-env.html` في المتصفح لرؤية متغيرات البيئة.

## 📝 ملاحظة:
الآن يمكنك تغيير رابط API بسهولة من ملف `.env` دون تعديل الكود!
