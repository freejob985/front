# 🚀 ابدأ من هنا - دليل سريع لمتغيرات البيئة

## ✅ تم حل المشكلة!

المشكلة: **`ERR_CONNECTION_REFUSED`** - التطبيق كان يستخدم مسارات ثابتة.  
الحل: **الآن يقرأ جميع المسارات من ملف `.env`**

---

## 🎯 3 خطوات بسيطة للبدء

### 1️⃣ تحديث ملف `.env`

افتح ملف `.env` في جذر المشروع:

```env
VITE_API_URL=http://localhost:8000/api/v1
VITE_API_MODE=direct
```

💡 **غيّر `8000` إلى منفذ خادمك إذا كان مختلفاً**

### 2️⃣ شغّل خادم Laravel

```bash
cd path/to/backend
php artisan serve --port=8000
```

### 3️⃣ شغّل التطبيق

```bash
npm run dev
```

---

## ✅ تحقق من نجاح الإعداد

افتح Console في المتصفح (F12)، يجب أن ترى:

```
✅ Security Config - Using VITE_API_URL from environment: http://localhost:8000/api/v1
```

---

## 📁 ملفات التوثيق

| الملف | متى تقرأه |
|-------|-----------|
| `API_ENV_VARIABLES_FIX_SUMMARY.md` | **ابدأ من هنا** - ملخص شامل |
| `QUICK_FIX_API_ENV.md` | حل سريع للمشاكل الشائعة |
| `ENV_VARIABLES_GUIDE.md` | دليل مفصل لجميع المتغيرات |
| `تعليمات_اصلاح_متغيرات_البيئة.md` | شرح مفصل بالعربية |

---

## 🛠️ الأوامر المفيدة

```bash
# اختبار التكوين
cmd /c test-env-config.bat

# إعادة إنشاء ملف .env
copy env.local .env

# مسح cache وإعادة التشغيل
npm run clean
npm run dev
```

---

## ❓ مشاكل شائعة

### ❌ لا يزال الخطأ `ERR_CONNECTION_REFUSED`

**السبب:** خادم Laravel غير قيد التشغيل أو على منفذ مختلف.

**الحل:**
1. تأكد من تشغيل `php artisan serve`
2. تحقق من المنفذ في `.env`
3. أعد تشغيل `npm run dev`

### ❌ التغييرات لا تظهر

**السبب:** لم تُعد تشغيل الخادم بعد تعديل `.env`.

**الحل:**
```bash
# اضغط Ctrl+C ثم
npm run dev
```

---

## 🎯 التغييرات الرئيسية

| قبل ❌ | بعد ✅ |
|--------|--------|
| مسارات ثابتة في الكود | قراءة من `.env` |
| تعديل ملفات متعددة | تعديل ملف واحد |
| build مختلف لكل بيئة | نفس build + `.env` مختلف |

---

## 📞 تحتاج مساعدة؟

1. راجع `API_ENV_VARIABLES_FIX_SUMMARY.md` للتفاصيل الكاملة
2. شغّل `cmd /c test-env-config.bat` لاختبار التكوين
3. تحقق من رسائل Console في المتصفح

---

**ملاحظة مهمة:** بعد أي تعديل في `.env` يجب إعادة تشغيل خادم التطوير! ✨






